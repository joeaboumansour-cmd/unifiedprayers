# dependencies / build output
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# local env
.env*.local

# decoded Claude Design bundle: 3 MB of vendored React/Babel blobs.
# The design source itself is kept in design/.
design-src/

# the previous static app, superseded by this one (kept on disk only)
unifiedprayers/

# OS noise
.DS_Store
Thumbs.db
